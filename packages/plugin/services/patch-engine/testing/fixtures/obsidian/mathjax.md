# MathJax Fixtures

## Inline Math

The famous equation $E = mc^2$ changed physics forever.

Greek letters: $\alpha + \beta = \gamma$ and $\sum_{i=1}^{n} x_i$.

A fraction: $\frac{a}{b}$ and a square root: $\sqrt{x^2 + y^2}$.

## Block Math

$$
\int_{-\infty}^{\infty} e^{-x^2} \, dx = \sqrt{\pi}
$$

$$
\mathbf{F} = m\mathbf{a} = m \frac{d\mathbf{v}}{dt}
$$

$$
\begin{aligned}
\nabla \cdot \mathbf{E} &= \frac{\rho}{\varepsilon_0} \\
\nabla \cdot \mathbf{B} &= 0 \\
\nabla \times \mathbf{E} &= -\frac{\partial \mathbf{B}}{\partial t} \\
\nabla \times \mathbf{B} &= \mu_0 \mathbf{J} + \mu_0 \varepsilon_0 \frac{\partial \mathbf{E}}{\partial t}
\end{aligned}
$$

## Matrix Equation

$$
\begin{pmatrix}
a_{11} & a_{12} \\
a_{21} & a_{22}
\end{pmatrix}
\begin{pmatrix}
x_1 \\
x_2
\end{pmatrix}
=
\begin{pmatrix}
b_1 \\
b_2
\end{pmatrix}
$$

## Math in Lists

- The derivative: $\frac{dy}{dx} = \lim_{h \to 0} \frac{f(x+h) - f(x)}{h}$
- Taylor series: $f(x) = \sum_{n=0}^{\infty} \frac{f^{(n)}(a)}{n!}(x-a)^n$
- Euler's identity: $e^{i\pi} + 1 = 0$

## Math in Blockquotes

> The Schrödinger equation:
> $$i\hbar \frac{\partial}{\partial t} \Psi = \hat{H} \Psi$$
> describes quantum state evolution over time.

> For the résumé of thermodynamics:
> $\Delta S \geq 0$ (second law) and $\Delta U = Q - W$ (first law).

## Math with Unicode and Multi-Byte Text

The $\text{résumé}$ of the proof shows $\alpha_{\text{über}} = 1$.

Considering the $\text{日本語}$ notation: $\mathcal{H}_{\text{中文}} = \sum_k E_k$.

In the $\text{café}$ problem, we solve $x^2 + \text{ñ} = 0$ symbolically.

## Wiki-Links with Multi-Byte Characters

See [[Résumé du Projet]] for the full derivation of $E = mc^2$.

The notes in [[日本語ノート#数学セクション]] cover $\int_0^1 f(x)\,dx$.

Refer to [[Über Thème|Thème Mathématique]] for $\nabla^2 \phi = 0$.

The [[中文笔记#方程式]] page documents $\frac{\partial^2 u}{\partial t^2} = c^2 \nabla^2 u$.

## Combined Block

$$
\text{Let } \mathcal{R}\text{ésumé}(x) = \int_0^x \frac{e^{-t}}{1+t^2}\,dt
$$
