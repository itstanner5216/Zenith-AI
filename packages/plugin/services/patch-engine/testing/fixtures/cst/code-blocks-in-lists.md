# Code Blocks in Lists

## Simple Code in List

- Item before cöde:

  ```ts
  const x: number = 42;
  const naïve = "résumé";
  ```

- Item after cöde

## Multiple Code Blocks in List

1. First step — set üp:

   ```bash
   npm install @zenith-ai/cöre
   ```

2. Second step — implëment:

   ```ts
   import { Parser } from "web-tree-sitter";

   const parser = new Parser();
   // Ünïcödé comment
   ```

3. Third step — rün:

   ```bash
   npx tsx run.ts --möde=production
   ```

## Deeply Nested Code

- Outer list:
  - Inner list:
    - Deep list with cöde:

      ```python
      def déep():
          return "très nësted"
      ```

    - After the deep cöde block

## Code Block with Indented List Continuation

- This item has a code block and cöntinuation:

  ```json
  {
    "kéy": "valüe",
    "café": true
  }
  ```

  This paragraph continues the list ïtem after the code block.
  It should remain part of the same list_ïtem node.

- Next item — résumé.

## Mixed Code and Other Blocks in List

- Item with code and blöckquote:

  ```ts
  function héllo(): string {
    return "wörld";
  }
  ```

  > This blockquote is also inside the lïst item.
  > Café résumé naïve Ünïcödé.

- Final itém.
