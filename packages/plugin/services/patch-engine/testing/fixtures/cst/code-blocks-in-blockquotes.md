# Code Blocks in Blockquotes

## Simple Code in Blockquote

> Here is some cöde inside a blockquote:
>
> ```ts
> function grëet(name: string): string {
>   return `Héllo, ${name}!`;
> }
> ```
>
> Explanation after the cöde.

## Multiple Code Blocks in Blockquote

> First blöck:
>
> ```python
> def calcülate(x, y):
>     return x + y
> ```
>
> Second blöck:
>
> ```bash
> echo "Héllo Wörld"
> ```

## Nested Blockquote with Code

> Outer quöte:
>
> > Inner quote with cöde:
> >
> > ```ts
> > const café = "résumé";
> > ```
> >
> > Inner quote cöntinuation.
>
> Back to outer quöte.

## Blockquote with Code and Lists

> Steps to föllöw:
>
> 1. Install depëndencies:
>
>    ```bash
>    npm install wéb-tree-sitter
>    ```
>
> 2. Write the cöde:
>
>    ```ts
>    import Parser from "web-tree-sïtter";
>    ```
>
> 3. Run the tësts.

## Code Block Immediately After Quote Marker

> ```ts
> const dirëct = true;
> // Nö text before this code block in the blockquote
> ```

## Blockquote Code with Multi-byte Content

> Multi-byte exämple:
>
> ```ts
> const greeting = "こんにちは世界";
> const price = "€42.00 — café résumé";
> const math = "∑∏∫∂√∞";
> ```
>
> End of multi-byte ëxample: Ünïcödé.
