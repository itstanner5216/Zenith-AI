# Vertex AI Search + Obsidian Gateway Service
# Python 3.12+ — pinned for reproducible builds

# FastAPI and server
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5

# HTTP client with HTTP/2
httpx[http2]==0.28.1

# Google Cloud authentication
google-auth==2.38.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# Multipart form support
python-multipart==0.0.20

# Redis client for session storage
redis==5.2.1

# PostgreSQL async client for pgvector
asyncpg==0.30.0

# MCP server — exposes all FastAPI routes as MCP tools (Fix 2)
fastapi-mcp>=0.4.0
